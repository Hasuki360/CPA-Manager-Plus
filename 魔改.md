# CPA Manager Plus 魔改说明

> 本文档记录 Hasuki 对 CPAMP 的全部定制扩展、二次开发与合并日志。

## 2026-09-08 完善 Codex 请求头自动更新调度与持久化 + 移植三项独立小修复 (v1.11.12-hasuki)

- **Codex 请求头同步后台自愈与调度增强（Go）**：
  - 修复空目标持久化丢失：移除 `AutomationSettings.CharityModelMonitorSites` 的 `omitempty`，使清空站点列表 `[]` 能正确持久化写入数据库，防止 Reload 重启时被还原为默认全 Codex 站点。
  - 修复同步中保存配置漏执行与取消时序：在 `CharityModelMonitorWorker` 中引入优雅取消与事件通知通道，合并并发保存请求，确保运行中保存配置不会丢失触发或漏跑最新目标。
  - 修复保存策略时的状态丢失：保存 `account-processing-policy` 时保留上次完成的同步状态，避免附加状态读取瞬态失败导致误报或状态卡消失。
- **Codex 请求头前端配置卡片（Web）**：
  - 保持旧版界面布局，增加保存后的平滑刷新机制，防止延迟 GET 覆盖手动刷新状态。
  - 接入真实提供商匹配与更新计数，避免按分组统计导致的渠道数量口径偏差。
  - 严格限定同步检查间隔在 5～10080 分钟之间的正整数，非法值前端直接阻断。
- **小修复 1：用量分析 Fallback API Key 筛选与趋势隔离（PR #657）**：
  - 针对历史数据中生成的兜底合成 API Key（`unknown-client-api-key:***`），从 API Key 筛选下拉菜单、趋势序列和监控详情链接中全面排除，仅保留其在总用量汇总中的贡献，避免污染真实 Key 的统计与图表。
- **小修复 2：模型价格同步反馈剔除已有价格模型（PR #595）**：
  - 模型价格同步完成后，从待确认候选列表（Candidates）与未匹配列表（Unmatched）中剔除数据库中已有价格的模型，避免虚假提示和待处理计数误导。
- **小修复 3：Codex 面板配额查询请求头 UA 对齐（Commit e010474c）**：
  - 更新面板自主查询 Codex 额度的客户端标识常量 `CODEX_REQUEST_HEADERS`，对齐官方最新 `codex-tui` 标识。

## 2026-09-06 恢复并增强 Codex 请求头自动更新功能（通配支持 + 界面控制面板）

- **功能恢复与通配增强**：
  - 后端 worker `charity_model_monitor.go` 增加 `*` 通配支持：当站点配置中 `codexBaseUrl: "*"` 时，自动匹配 `codex-api-key` 分组下的全部 Codex 提供商，将其 `User-Agent: codex_cli_rs/<最新版本> (Mac OS 26.6.0; arm64)`、`originator: codex_cli_rs`、`x-openai-subagent: codex-mcp-client` 同步为 npm registry 上的最新版本。
  - 仅同步请求头模式（`syncCodexHeadersOnly: true`）保证不改动提供商通道的启用/封锁状态。
  - 增加空配置保护：当上游 CPA 未添加任何 Codex 提供商时，优雅返回 `no providers configured`，不再输出误报错误。
  - 修复 HTTP PATCH 触发 Reload 时的 context canceled 问题：在 `UpdateConfig` 中启动异步同步时解耦请求上下文，并增加 `runMu.TryLock()` 互斥保护，防止并发重叠执行。
- **界面与文案优化（配置面板）**：
  - 在「配置面板 -> 账号处理策略」加回扩展分组卡片，名称更新为「**自动更新 Codex 请求头**」（前端展示与存储/API 协议解耦）。
  - 卡片集成实时状态展示（上次检查时间、当前检测到的最新版本号、上轮同步通道统计）以及高级配置编辑区（同步间隔与站点 JSON 自定义，支持在线保存即时生效）。
- **时间周期调整**：
  - npm 官方源最新 Codex CLI 版本号拉取与缓存周期由 6 小时调整为 **两天（48 小时）**（`charityMonitorCodexVersionTTL = 48 * time.Hour`）。
  - 后台通道请求头巡检同步间隔由 15 分钟调整为 **一天（1440 分钟）**（`DefaultCharityModelMonitorIntervalMinutes = 1440`），两个实例已全部实机应用。
- **验证与部署**：
  - 后端 Go 单元测试全绿（含通配匹配与空配置边界测试）。
  - 前端 Vitest 单元测试全绿（134 文件 / 1265 测试 pass）及 TypeScript `tsc --noEmit` 通过。
  - 两个实例（18317、18318）均已完成新镜像构建部署并开启该功能，实测成功将全部 4 个 Codex 提供商自动更新至 npm 最新版本 `0.153.4`。

## 2026-09-01 移植上游 v1.12.7 核心底层稳定性与数据层修复 (旧版用新版功能)

> **核心原则（重要提醒）**：
> 本轮以及后续所有更新，均严格执行 **「旧版界面基线（v1.11.12-hasuki）+ 精准挑取新版实用能力/底层稳定性」** 模式。
> 严禁引入上游 v1.12+ 的 Accounts 卡片工作区大改版界面，严禁破坏已有的 🟢 缓存成功状态、百分比阶梯着色、中文锁定与 Antigravity 定制冷却。

- **请求失败体与响应头彻底分离（PR #619）**：
  - 移除了请求失败时将 Response Headers 混合拼接进 `fail_body` 的遗留行为，保证错误日志和数据库中的 `fail_body` 保持纯净的 JSON/错误文本。
  - 本地增强：保留了从顶级 payload 提取 `error` / `error_message` 并紧凑化为 JSON 的兼容逻辑（用于 OneAPI / New-API 等 500 熔断提取）。
- **嵌入式面板静态缓存协商（PR #586）**：
  - 嵌入式面板（`management.html`）返回时添加 SHA-256 ETag 与 `Cache-Control: no-cache` 协商校验头，支持 `304 Not Modified`，大幅减少页面重复加载开销并加速 Docker 升级后的即时刷新。
- **用量与监控时间线 0 值时间桶补齐与夏令时避坑（PR #637）**：
  - 规范化用量统计主时间线，自动为没有调用的时间区间生成 0 值时间桶，避免图表断点与空点计算异常；
  - 保留空时间线无样本语义，优化图表比率与延迟异常对比；
  - 修正按本地小时推进的逻辑，避免时区/夏令时回拨时合成重复的时间桶。
- **配额冷却周期边界与外部重新启用对齐（PR #640）**：
  - 对齐 Quota Cooldown 周期边界计算；
  - 凭据被外部手动或系统重新启用后，自动开始全新的冷却观察周期。
- **SQLite WAL 事务读取清理与启动环境诊断（PR #607 / PR #655）**：
  - 升级 `modernc.org/sqlite` 驱动并完善取消上下文时的 WAL 事务释放，防止高并发只读查询锁库或 WAL 无限增长；
  - 增加启动阶段 SQLite 权限与临时目录（`SQLITE_IOERR_GETTEMPPATH`、`SQLITE_READONLY`）精准诊断提示。
- **测试与验证**：
  - 后端 Go 单元测试全绿（`go test -count=1 ./...` 全部 pass）。
  - 前端 Vitest 单元测试与 TypeScript 类型检查全绿（`134` 文件 / `1264` 测试通过，`tsc --noEmit` 通过）。

## 2026-08-23 移植上游配置、提供商与稳定性增强 + Antigravity 429 1分钟短冷却 (b00989e7)

> **核心原则（重要提醒）**：
> 本轮以及后续所有更新，均严格执行 **「旧版界面基线（v1.11.12-hasuki）+ 精准挑取新版实用能力/底层稳定性」** 模式。
> 严禁引入上游 v1.12+ 的 Accounts 卡片工作区大改版界面，严禁破坏已有的 🟢 缓存成功状态、百分比阶梯着色与 Antigravity 定制冷却。

- **Antigravity 429 一分钟瞬时短冷却（新增定制）**：
  - 当 Antigravity 遇到 HTTP 429 `RESOURCE_EXHAUSTED` 时：
    - 若后台反查套餐配额确认归零，按官方 `resetTime` 进行精准长冷却恢复；
    - 若配额仍有剩余（属于瞬时并发/RPM 冲顶）或查询配额无明确归零桶，自动应用 **1 分钟短冷却（`cpamp_antigravity_429`）** 停用并倒计时 1 分钟后自动恢复，避免并发时持续撞墙报错。
- **AI 提供商与配置面板增强（旧版用新版特性）**：
  - 支持多 Key / 凭据权重路由（Weighted Credential Routing）与加权轮询调度（`weighted-round-robin`）。
  - 修复可视化配置面板（VisualConfigEditor）中路由策略加权轮询键名（`strategy_weighted_round_robin` / `加权轮询 (Weighted Round Robin)`）在各语言中的补全。
  - 接入 CPA 三态冷却策略选择器（CoolingPolicySelect，跟随全局 / 显式启用 / 显式禁用）。
  - 支持模型发现和健康检查通过配置的代理（proxyUrl）转发，避免直连超时。
- **后端底层稳定性与并发安全（旧版用新版特性）**：
  - **SQLite 进程级文件互斥锁（Process Lock）**：通过 `golang.org/x/sys` 为数据库文件添加跨平台进程排他锁（`.manager.lock`），从根源杜绝多进程或并发写入损坏 DB。
  - **推理模型后缀与解析模型分离**：在用量统计与实时监控层区分原始请求模型名（如 `deepseek-v4-flash(max)`）与实际上游解析模型名（`resolvedModel`），精准归类统计。
  - **CPA 插件样式隔离放宽**：采用 `:where(html[data-cpamp-plugin-host='true'])` 降低宿主样式注入特异性，避免第三方插件样式被基础全局样式覆盖。
- **仪表盘增强**：
  - 版本卡片支持点击跳转 GitHub Releases 链接。
- 所有单元测试（前端 Vitest 134 文件 / 1255 测试，后端 Go 测试全绿）均通过，已构建并热重启 18317、18318 双实例。

## 2026-08-12 基线 v1.11.12 & 删除凭证健康巡检 & Antigravity 汉化

- 代码基线：上游稳定版 **`v1.11.12`**（commit `68b57da8`），已退回并锁定基线，不追 v1.12 界面。
- **删除「凭证健康巡检」（CodexInspection）**：
  - 前端：巡检页面/组件/模型层、侧边导航、路由、Demo 相关全部删除。
  - 后端：worker、service、controller、repository、model 全部删除；managerconfig/proxy 中巡检配置与 ownership 逻辑移除（proxy 保留锁机制调用链，store 读写改为空操作）。
  - DB：`codex_inspection_*` 三张表保留不主动 DROP（迁移脚本不动，代码不再读写）。
- **Antigravity 套餐英文汉化修复**：bucket label 增加正则推断兜底（`Five Hour Limit Remaining` → 5 小时限额、`Weekly Limit Remaining` → 周限额），不再精确匹配英文原文。
- **Antigravity 503 五分钟短冷却**：503 服务端超时临时停用认证文件 5 分钟后自动恢复，独立 owner `cpamp_antigravity_503`；429 仍走套餐额度核实长冷却。
- 状态列 `🟢 缓存成功`、不四舍五入两位小数命中率、80-100 绿/50-80 橙/1-49 红阶梯着色、界面锁定简体中文（zh-CN）均保留。

## 历史

- 2026-08-11 合并上游 v1.12.0-beta2 后因界面不喜欢退回 v1.11.12。
- 中文用量单位 `千/万/亿/万亿` 与 5000万 进位避坑、末尾 `.0` 抹除。
