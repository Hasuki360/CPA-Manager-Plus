# CPA Manager Plus 魔改说明

> 本文档记录 Hasuki 对 CPAMP 的全部定制扩展、二次开发与合并日志。

## 2026-08-11 基线退回 v1.11.12 & Antigravity 503 五分钟短冷却

- 代码基线：已将上游代码基线从 `v1.12.0-beta2` 完整退回到 **`v1.11.12`**（commit `68b57da8`）。
- 新增魔改：**Antigravity HTTP 503 五分钟短冷却自动恢复**
  - 收到 Antigravity `503 UNAVAILABLE`（如 Deadline expired 服务端超时）时，自动禁用对应认证文件，**5 分钟**后自动恢复（`cpamp_antigravity_503` owner）。
  - 与 429 额度核实长冷却（`cpamp_usage_429`）完全隔离：503 不查套餐额度、固定 5 分钟；429 仍走 `/v0/management/api-call` 额度确认 + 套餐窗口 resetTime 恢复。
  - 恢复队列已放行 `cpamp_antigravity_503` owner（`recoverCooldown`）。
- Hasuki 专属魔改全量保留：
  - **Antigravity 额度冷却处理**：保留 Antigravity 404/429 套餐额度核实与冷却开关。
  - **公益站模型监控与通道自愈**：保留薄荷/AnyRouter 监控 worker、模型 `excluded-models` 自愈与面板卡片。
  - **请求监控状态列 `🟢 缓存成功`**：当命中/读取缓存时显示 `🟢 缓存成功`（判定条件：`cachedTokens > 0 || cacheReadTokens > 0`）。
  - **动态阶梯着色缓存命中率**：在 `🟢 缓存成功` 下方显示不四舍五入直接截取的两位小数百分比：
    - **80% ~ 100%**：🟢 绿色 (`var(--monitor-green)`)
    - **50% ~ 79.99%**：🟠 橙色 (`var(--monitor-amber)`)
    - **1% ~ 49.99%**：🔴 红色 (`var(--monitor-red)`)
  - **中文数值单位与 5000万 避坑**：`千/万/亿/万亿` 转换，抹除末尾 `.0`，防止 5000万 被提升为 `0.5亿`。
  - **系统语言锁定**：系统语言锁定为 **简体中文** (`zh-CN`)。
- 验证与部署：`go vet` 干净、后端 `go test ./...` 全绿、Vitest 135 文件 / 1373 项单测全绿、`tsc` 0 报错；构建 `v1.11.12-hasuki-503` 镜像并 `force-recreate` 部署上线，端口 `18317` healthy。
